////////////////////////////////////////////////////////////////////////////////
// Small async helpers for demo-only dashboard surfaces
////////////////////////////////////////////////////////////////////////////////

export const delay = (ms: number) => new Promise((resolve) => setTimeout(resolve, ms));
